#!/bin/sh
set -e

# Exécuté dans /git-build
ROOT="$(cd .. && pwd)"

mkdir -p "$ROOT/httpdocs" "$ROOT/app" "$ROOT/config"
/bin/cp -a "httpdocs/." "$ROOT/httpdocs/"
/bin/cp -a "app/."      "$ROOT/app/"
/bin/cp -a "config/."   "$ROOT/config/"
echo "OK"
